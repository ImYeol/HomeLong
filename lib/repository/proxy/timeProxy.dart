class TimeProxy {
  void updateHomeTime(
      DateTime enterTime, DateTime exitTime, DateTime targetDay) {}

  int getTotalMinuteADay(DateTime targetDay) {
    int totalMinuter = 0;
    return totalMinuter;
  }
}
